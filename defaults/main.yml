---

#
# variables needed to be defined in user's playbook
#



#
# user-configurable defaults
#

monit_version:              5.17.1

monit_port:                 2812
monit_interval_in_seconds:  60

monit_install_path:      /usr/bin
monit_config_path:       /etc/monit.d
monit_db_path:           /var/lib/monit

monit_download_path:     /tmp

monit_clean_tarball:     true


#---- the following vars are handled in tasks/set-role-variables.yml ------

#monit_use_service:       True      # use "service" command to start/restart monit?
